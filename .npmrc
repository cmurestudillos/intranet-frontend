auto-install-peers=true
strict-peer-dependencies=false
# Ignora lifecycle scripts (postinstall, etc.) de las dependencias instaladas.
# NO afecta a los scripts de usuario definidos en package.json (dev, build, lint).
ignore-scripts=true
